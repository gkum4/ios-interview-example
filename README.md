# iOS Inverview Example

Example of an iOS live coding interview with buggy code that should be fixed and new features to implements.

- Uses [Disney API](https://disneyapi.dev/) to show a list of characters.
- UIKit with ViewCode.
- MVVM architecture.